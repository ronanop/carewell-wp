/**
 * page-renderer invariants — Experience AST path removed in Sanity cutover.
 * Kept as a no-op smoke so the script entry still exits 0.
 */
console.log("[page-renderer] invariants skipped (AST/Experience removed)");
process.exit(0);
