"use client";

/**
 * Re-export shared scroll reveal for doctor profile chapters.
 * Same IO + CSS motion as the About page (no Framer).
 */
export { AboutReveal as DoctorReveal } from "@/components/about/AboutReveal";
