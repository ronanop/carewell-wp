/**
 * Page ExperienceContentProvider — delegates to Service Experience Provider (Phase 8.0).
 * Kept as a named alias for older imports; editorial path lives in serviceProvider.
 */

export { createServiceExperienceProvider as createPageExperienceProvider } from "@/lib/experience/unified/providers/serviceProvider";
